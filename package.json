{
  "name": "r-instant-preview",
  "displayName": "R Instant Preview",
  "description": "Instantly preview selected R code results inline and in a dedicated panel with smart context analysis.",
  "version": "0.1.0",
  "publisher": "linxiangsun145",
  "icon": "images/icon.png",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/linxiangsun145/r-hidden-preview.git"
  },
  "bugs": {
    "url": "https://github.com/linxiangsun145/r-hidden-preview/issues"
  },
  "homepage": "https://github.com/linxiangsun145/r-hidden-preview#readme",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Programming Languages",
    "Other"
  ],
  "keywords": [
    "R",
    "preview",
    "Rscript",
    "selection"
  ],
  "activationEvents": [
    "onLanguage:r",
    "onCommand:rHiddenPreview.openPreviewPanel",
    "onCommand:rHiddenPreview.showOutput"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "rHiddenPreview.openPreviewPanel",
        "title": "R Hidden Preview: Open Preview Panel"
      },
      {
        "command": "rHiddenPreview.showOutput",
        "title": "R Hidden Preview: Show Output"
      }
    ],
    "configuration": {
      "title": "R Hidden Preview",
      "properties": {
        "rHiddenPreview.rscriptPath": {
          "type": "string",
          "default": "Rscript",
          "description": "Path to Rscript executable. You can use a full path if Rscript is not in PATH."
        },
        "rHiddenPreview.timeoutMs": {
          "type": "number",
          "default": 3000,
          "minimum": 300,
          "description": "Timeout in milliseconds for each background preview execution."
        },
        "rHiddenPreview.debounceMs": {
          "type": "number",
          "default": 350,
          "minimum": 50,
          "description": "Debounce delay in milliseconds before triggering preview after selection changes."
        },
        "rHiddenPreview.autoPreview": {
          "type": "boolean",
          "default": true,
          "description": "Enable automatic preview on selection changes."
        },
        "rHiddenPreview.enableHoverPreview": {
          "type": "boolean",
          "default": false,
          "description": "Enable optional hover preview. When enabled, hovering R expressions shows quick preview like IntelliSense."
        },
        "rHiddenPreview.safeAutoExecutionOnly": {
          "type": "boolean",
          "default": true,
          "description": "If enabled, auto preview only runs for expressions passing the built-in safe rule engine (isSafeExpression)."
        },
        "rHiddenPreview.safeFunctionBlacklist": {
          "type": "array",
          "default": [
            "read.csv",
            "read.table",
            "read.delim",
            "write.csv",
            "write.table",
            "install.packages",
            "system",
            "system2",
            "shell",
            "source",
            "download.file",
            "unlink",
            "file.remove",
            "file.copy",
            "save",
            "saveRDS",
            "load",
            "setwd"
          ],
          "items": {
            "type": "string"
          },
          "description": "Function call blacklist used by safe auto execution (case-insensitive). Any matched function call will be skipped."
        },
        "rHiddenPreview.safeFunctionWhitelist": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          },
          "description": "Optional function call whitelist for safe auto execution (case-insensitive). When non-empty, only listed function calls are allowed."
        },
        "rHiddenPreview.contextMode": {
          "type": "string",
          "default": "documentBeforeSelection",
          "enum": [
            "selectionOnly",
            "documentBeforeSelection",
            "smartContext"
          ],
          "markdownDescription": "Execution context mode. `selectionOnly`: run only selected code. `documentBeforeSelection`: run code from document start to selection start, then run selected code. `smartContext`: analyze selected expression dependencies and execute minimal required upstream assignments first, then automatically fallback to wider context if validation fails in the long-lived R session."
        },
        "rHiddenPreview.maxOutputLength": {
          "type": "number",
          "default": 2000,
          "minimum": 100,
          "description": "Maximum output length for summary and detail text. Extra output is truncated."
        },
        "rHiddenPreview.showInlinePreview": {
          "type": "boolean",
          "default": true,
          "description": "Show one-line summary as inline end-of-line preview decoration."
        },
        "rHiddenPreview.showPanelPreview": {
          "type": "boolean",
          "default": true,
          "description": "Show detailed preview in a dedicated detail view beside the editor."
        },
        "rHiddenPreview.requireWorkspaceTrust": {
          "type": "boolean",
          "default": true,
          "description": "If enabled, auto preview runs only in trusted workspaces."
        },
        "rHiddenPreview.ignoreIncompleteSelection": {
          "type": "boolean",
          "default": true,
          "description": "If enabled, skip execution when selection looks incomplete (for example unbalanced brackets or unfinished expression)."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test:smart-context": "npm run compile && node out/test/smartContext.test.js",
    "test:incremental": "npm run compile && node out/test/incrementalExecution.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.14.15",
    "@types/vscode": "^1.90.0",
    "typescript": "^5.5.4"
  }
}
